import Header from "../Pages/Header";
import Navigation from "../Pages/Navigation";

const Layout = () => {
    return (
        <div>
            <Navigation />
            <Header />
        </div>
    );
};

export default Layout;
